#include <stdlib.h>

// Program will very simply take in an imput text file and compute
// the distance between every city as though there x and y coordinates
// are on a euclidian space (this is a bad example overall)

// lines marked with # can be ignored
// values will be seperated with spaces
// entries will be seperated by line breaks '\n'

// files will be output in a matrix format of distances

// main will simply take in the file and output the results
// passing all other computation to helper functions
int main(int argc, char * argv[]){
    return 0;
}